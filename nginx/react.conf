server {
    listen 80;
    server_name localhost;
    
    root /usr/share/nginx/html;
    index index.html;

    # 👇 这就是核心魔法：找不到文件就返回 index.html
    location / {
        try_files $uri $uri/ /index.html;
    }
}